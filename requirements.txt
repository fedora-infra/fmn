beautifulsoup4
docutils
fmn.rules
# html5lib is required because lib currently contains the code that does
# markup for the web app. It should be moved to fmn.web and this dependency
# can then be removed.
html5lib
markupsafe
six
SQLAlchemy>=0.8
